# Copy to .env in this folder. Keys must be prefixed with VITE_ to be exposed to the client.
# Never commit .env. For production, call APIs from a server—do not ship keys to browsers.

VITE_OPENAI_API_KEY=
